import React from 'react'
import { styles } from '../assets/style'

const Dashboard = () => {
  return (
    <div className={`${styles["bg-secondary"]} h-full w-full flex justify-center items-center rounded-3xl`}>Dashboard</div>
  )
}

export default Dashboard